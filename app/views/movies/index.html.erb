<h1>Welcome to the Movie Collection Management App!</h1>
<table class="table">
  <thead>
    <tr>
      <th>Moive</th>
      <th>Genres</th>
      <th>Views</th>
    </tr>
  </thead>
  <tbody>
  <% @movies.each do |movie| %>
    <tr>
      <td><%= "#{movie.title} by #{movie.director} (#{movie.release_year})" %></td>
      <td><%= "genres - #{movie.genres}" %></td>
      <td><%= "Views #{movie.view_count}" %></td>
    </tr>
  <% end %>
</tbody>
   
</table>